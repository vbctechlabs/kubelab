apiVersion: v1
kind: Pod
metadata:
  name: resultapppod
  labels:
    name: result-app-pod
    app: voting-app
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80